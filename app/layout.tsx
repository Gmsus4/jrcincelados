import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Cincelado Artesanal | Accesorios de Cuero Hechos a Mano",
  description: "Descubre cinturones, fundas y accesorios de cuero auténtico, elaborados a mano con técnicas de cincelado tradicional. Calidad, durabilidad y arte en cada pieza.",
  icons: {
    icon: "/favicon.webp"
  }
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
